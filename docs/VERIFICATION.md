# Verified and unverified

Every claim this project makes is in one of two tables. The top table has
evidence you can check today. The bottom table does not.

Two ways to help. Run an unverified row on hardware we do not have and move it
up. Or tell us where the design is wrong.

Most of the unverified rows need one thing: hardware that is not a developer
laptop. A Raspberry Pi, a board with no GPU, a machine with 4 GB. We do not have
enough of those, and CI has none.

**To claim a row:** open the linked issue, say which board you have, run the
steps, and paste the output. Pass or fail both help. A row that fails moves to
the top table as a verified negative, with the failure recorded, which is worth
as much as a pass.

## Design review

Better designs exist for most of what is here. The scheduler, the model bus, the
session supervisor, the peer transport, the tier ladder were all built by people
solving the problem in front of them.

If you see a better one, open an issue, name the file, make the argument. No
benchmark required.

Design compounds in an OS because everything above a choice inherits it. That
makes this worth more than another feature.

---

## Verified

| Claim | Evidence | Last checked |
|---|---|---|
| The compositor builds with Smithay linked, talking DRM and KMS | `nix-build-matrix.yml`, M9 gate `hart-comp`, `buildFeatures = ["smithay"]` | 2026-07-26, success |
| The desktop ISO builds and publishes in four parts | `release.yml` -> `build-iso (iso-desktop)` | 2026-07-27, success |
| The flake evaluates on every push to `nixos/**` | `nix-check.yml` | 2026-07-27, success |
| The co-pilot cannot escalate privilege or change the boot default | 19 tests, `tests/unit/test_copilot_daemon_boundary.py` | 2026-07-27, pass |
| Screen recording produces a real mp4 at the rate it captured | `hart desktop record`, measured -0.6% timeline drift over 4s | 2026-07-27 |
| Constitutional rules are enumerable without running anything | `security/hive_guardrails.py`, `CONSTITUTIONAL_RULES` frozen tuple, 13 guardrail classes | 2026-07-27 |
| The hive weights a Pi and a GPU rack equally at equal participation | `federated_aggregator.py:642`, `log1p(interactions)`, no tier multiplier | 2026-07-27 |
| The fleet binary cache serves signed HART system closures | `hart-nix-cache` on deepbox:8093, 5,575 signed paths / 12G, 57 `nixos-system-hart-node` toplevels, newest 11:37 | 2026-09-05, live |
| An OTA-applied generation would mount a raw-flashed disk | 28 of those 57 toplevels mount `/` by `hart-root` and `/boot` by `HART-ESP`; 0 carry per-rev labels. Read out of the cache: toplevel -> `-etc` -> `-etc-fstab`, zstd-decompressed | 2026-09-05, live |
| Both the ISO and the raw config reach the cache each run | The 57 arrive in 28 raw + 29 other pairs 1-2 min apart, which is `nix-build-matrix.yml`'s `cfgs="hart-desktop hart-desktop-raw"` loop | 2026-09-05, live |
| The raw generation registers its own boot entry | its `systemd-boot` builder carries `bootctl install/update` with `--graceful --no-variables` | 2026-09-05, live |
| A node can FETCH that generation from the substituter it ships with | `hart-base.nix` pins `http://etime.hertzai.com:8093`; that name resolves to 106.51.181.24 and returns HTTP 200 for `jzhhbf4d9hb2pdnis12cqlaz1myryyhj.narinfo`, the raw-bootable toplevel above, signed `hart-cache-1` which is the key pinned beside it | 2026-09-05, live |
| The stick carries the stable-label image, and it is THAT generation | Reflashed 2026-09-05 from the 2026-09-04 nightly. Flasher: stream sha256 == the published `.raw.sha256`, then a full device read-back == the stream. Its UKI's `.cmdline` names `jzhhbf4d9hb2pdnis12cqlaz1myryyhj`, the same toplevel verified above as mounting `hart-root`/`HART-ESP` | 2026-09-05, pass |
| **OTA applies, boots, and rolls back on hardware** | Full cycle on the Samsung box 2026-09-07. Fetched `yz36m179...` from `etime.hertzai.com:8093` in 50s (3 paths; rest shared), registered as generation 2, entries appeared in `/boot/loader/entries` with no manual work, rebooted in 148s onto generation 2, then `nix-env --rollback` + reboot in 63s back onto generation 1 | 2026-09-07, pass |
| A generation built from a DIFFERENT rev mounts the same disk | Same run: generation 2 booted with `/` on `/dev/disk/by-label/hart-root`. Under the per-rev labels this was unbootable by construction (stage 1 "must mount the root filesystem on /mnt-root"), which is the failure the cutover exists to fix | 2026-09-07, pass |
| First boot registers the rollback target and installs the bootloader | `[Profile] Registered generation 1 -> jzhhbf4d... (the rollback target OTA needs)` and `[Bootctl] systemd-boot installed/refreshed on the ESP`; `/boot/EFI/systemd/systemd-bootx64.efi` present. `/boot/loader/entries` was EMPTY before the first apply | 2026-09-07, pass |
| Two nodes form an ENCRYPTED PeerLink and messages cross it | `tests/standalone/peer_link_proof.py`, 11/11: upgrade in 1.92s, `encrypted=True`, server `active_links=1`, a gossip message reaches a handler on the far side | 2026-09-05, pass |
| A second node makes shard fan-out stop running single-node | same run, step 6: `hive_benchmark_prover._discover_nodes()` returns 2 nodes, 1 `type=peer_link` | 2026-09-05, pass |
| SAME_USER trust is grantable, so skill broadcast has a path | same run, steps 9 to 11: both sides grant `same_user`, and `federation.recipe_delta` (what `skill_exporter` publishes) arrives on the peer | 2026-09-05, pass |
| A node ACCEPTS a peer's signed announce and counts it | `tests/standalone/two_node_collaboration.py`: `accepted=True is_new=True`, B's own census reports 2 nodes with one `local=False` | 2026-09-05, pass |
| A learning delta produced on A is merged and counted by B | same run, step 4: delta accepted, read back FROM B rather than inferred from A's 200 | 2026-09-05, pass |
| A hive node bootstraps a newcomer better than a solo node | `tests/standalone/network_beats_solo_proof.py`: joiner inherits 2 community-validated heuristics pooled from 3 peers; the solo node hands over `{}` | 2026-09-05, pass |
| The experiment lifecycle advances on its own schedule without inventing outcomes | `tests/standalone/experiment_lifecycle_proof.py` 9/9, `decided` rows stay 0 | 2026-09-05, pass |
| No deployed shell route crashes on a plain request | `test_surface_drive_all.py` drives all 329 registered routes through a real `test_client`; every one returns a controlled status, zero 500s | 2026-09-05, pass |
| The shell surface works chapter by chapter with the OS boundary SEALED | `tests/integration/shell_surface/`, 449 pass: boot and first paint, session and personalize, system controls, network, apps and upgrades, events and sinks | 2026-09-05, pass |
| The earnings, health and delegation paths hold end to end | `test_compute_earnings_e2e.py`, `test_health_endpoints.py`, `test_task_delegation_bridge.py`, 22 pass | 2026-09-05, pass |
| An A2UI push wakes the SSE stream without waiting on a durable write | `test_liquid_ui_sse_event_driven.py` 4/4; the audit commit sat ahead of the wake and cost 5.8s to 11.5s per push, now 0.000s | 2026-09-05, pass |
| The native scene layer is not the frame-rate bottleneck | `scene_cost_fits_the_frame_budget` on an i7-7700K, release: a full relayout WITH real cosmic-text shaping is p99 219us, 1.3% of a 16.667ms frame; geometry alone is 2us | 2026-09-05, measured |
| The native compositor HOLDS as the live desktop | Cold boot on the Samsung box with the socket-wait fix: socket bound at 19.4s, first scanout 0.7s later, glass shell mapped as a Background layer surface, `layer.composited layers_painted=1`, and `current-tier=hart-comp` with `tier-degraded` absent | 2026-09-07, pass |
| Synthetic input reaches the compositor's seat on real hardware | A uinput device injected 2,228 pointer reports on 2026-09-07; hart-comp logged `New device "event10"` and `first seat input delivered - libinput/Seat path is LIVE`, so uinput -> evdev -> libinput -> seat is proven end to end | 2026-09-07, pass |
| Pointer motion over a static desktop costs ZERO frames | Same run: those 2,228 reports produced no repaint and no latency sample. Damage tracking working as designed, and the reason `hart-latency` prints nothing until input actually changes something | 2026-09-07, measured |
| The Tier-1 socket wait was what dropped the native tier | Three cold boots measured hart-comp binding at 14.6s / 22.5s / 19.4s against a 10s wrapper bound. The wrapper EXITS on timeout, so the glass shell was never launched at all and nothing could paint; the 120s watchdog then dropped a compositor that scans out 0.6s after binding | 2026-09-07, pass |
| hart-comp composites its own chrome on real hardware | Same boot: `orb.composed took_ms=2`, `bloom.composed width=1600 height=900 took_ms=66`, `native-chrome published chrome=bloom,orb` on GLES / Intel HD 4000 | 2026-09-07, measured |
| A steady desktop pays no layout at all | same run: a retained-tree hit is p50 and p99 both under 1us, and `rebuilds()` does not move on an unchanged key | 2026-09-05, measured |
| Windows / macOS capability parity is COMPUTED, not asserted | `OS_PARITY_MATRIX.md`, 30 rows, gated by `test_nixos_configs.py::TestParityMatrix`: 30 pass. 28 present, 2 deliberately partial (remote-desktop control and firewall writes are steward-gated ingress on purpose), 0 gaps | 2026-09-05, pass |
| The matrix cannot advertise a route that does not exist | same gate: every `/api/shell/...` the matrix cites is checked against the registered routes, and the honest-gap test parametrizes over the gap list, which is now EMPTY (hence its skip) | 2026-09-05, pass |
| The desktop's event channel opens immediately, not on the next keep-alive | `/api/notifications/stream` on the Samsung box, same probe before and after a one-line overlay: OPEN 15.024s (first frame `: hb`) -> OPEN 0.012s (first frame `: ok`), heartbeats unchanged at 15.012s / 30.013s. Werkzeug withholds the head until the first yield and the producer opened with a 15s CV wait, so every page load and reconnect paid a full heartbeat before `EventSource.onopen`. Fixed ebc84c1 | 2026-09-07, measured on hardware |
| HART-comp's window IPC works on real hardware | `com.hart.Compositor` on the Samsung box: `window.list` -> `ok:true`, an unknown method -> `code:unsupported`. The socket is `/run/user/1000/hart-comp.sock`, 0600, bound at compositor start under the DRM backend | 2026-09-07, measured on hardware |
| A banked window-layout recipe replays on the native tier | `replay_layout` through the new HART-comp transport, driven as the session user: `ok:true, replayed 3 of 3`, the workspace really switching to 2 and back. Before the transport existed the same recipe gave `available:true, replayed 0 of 3` with three reasonless `ok:false` | 2026-09-07, measured on hardware |
| The IPC relay carries HART-comp's protocol through the 0600 wall | the relay script extracted from `hart-layer-shell-host.nix`, run as root, driven the way systemd drives it (stdin/stdout ARE the client socket): a 71-byte framed reply came back intact. This is the script the unit execs, not a stand-in | 2026-09-07, measured on hardware |
| An OS app installs and LAUNCHES, under the service's own environment | the real `AppInstaller._install_nix`, unmocked, on the Samsung box, run with hart-liquid-ui.service's actual env (46 PATH entries, none of them `/run/current-system/sw/bin`; no `NIX_PATH`): the old code raised `FileNotFoundError` -> "nix-env not available", the new one installs, reports a real store path that exists, and the binary runs (`Hello, world!`). THREE stacked faults, each alone fatal: a channel-style argv on a node with no channels, `nix-env` unfindable on a unit PATH, and no `NIX_PATH` to resolve `<nixpkgs>`. All 176 installer tests fake the package manager, so none saw any of it | 2026-09-07, measured on hardware |
| The Nunba microfrontend is really proxied, not floored | on the box, `/social` through the shell answers `Server: hypercorn-h11` while `/` answers `Server: waitress`. So the UDS proxy to `hart-nunba` over `/run/hart/nunba.sock` is live and serving the daemon, not the bundled React fallback that stands in when the socket is unreachable | 2026-09-07, measured on hardware |
| The shell service reports its own features honestly | swept all 77 binaries the shell layer shells against hart-liquid-ui.service's REAL PATH on the box: 33 were installed and INVISIBLE to the unit (notify-send, xdg-open, xdg-mime, the clipboard tools, gsettings, wlr-randr, powerprofilesctl, git, gh, hart-self-build, sudo, and every nix binary), and ALL 29 other `hart-*` units had the same blindness. `run_probe` reads that as "optional tooling absent", so each degraded silently and the OS called its own working features unavailable. After the shared `system_search_path` fix, re-run under the unit's own PATH on the box: notify-send / xdg-open / gsettings / git / nix-env / wl-copy all REACHED, 6 of 6 | 2026-09-07, measured on hardware |
| Flatpak is wired correctly up to the download | on the box as the REAL service user (`hart`), under the unit's own PATH: `FLATPAK_USER_DIR` set, flatpak findable through the shared search path, `_ensure_flathub()` creates the remote, and `remote-info flathub org.gnome.Calculator` returns live metadata over the network (Download 1.8 MB, Runtime org.gnome.Platform/50). The DOWNLOAD is deliberately not exercised: no runtime is installed, so it would pull ~1GB onto a disk at 92% with 2.3G free, which collides with OTA headroom. That is a capacity decision, not a code question | 2026-09-07, measured on hardware |
| NodeWatchdog's background THREADS are healthy (not the same as daemon goals) | `/api/social/dashboard/health` on the box: 9 threads registered, all healthy and inside their thresholds (`expected_interval` x the 10.0 frozen-multiplier), watchdog check loop live, fleet_halted false. CORRECTION: I first reported this as "daemon goals proven working". It is not. These are gossip / sync_engine / model_lifecycle style background threads under the watchdog, NOT agent goals. `agent_status` reports `goals.total: 0` | 2026-09-07, measured on hardware |
| Synthetic input reaches the compositor over the REAL path | 104 Super+N workspace chords injected through /dev/uinput on the box, each one logged by hart-comp as `workspace.switched workspace=N restored=0`. That is uinput -> evdev -> libinput -> hart-comp's seat -> the Super+1..9 chord handler in comp_core.rs. The input half of input-to-photon needs no human | 2026-09-07, measured on hardware |
| The desktop burns a full core drawing what nobody sees | measured on the box 2026-09-07 with the native-chrome bridge FULLY engaged (verdict `bloom,orb`, shell logged `surface transparent`): `WebKitWebProcess` held **1383 CPU ticks in 14s, 98.8% of a core**, while `layer.composited` had fired ONCE the entire boot and the latency instrument recorded zero samples from 943 pointer plus 104 key events. The handoff stopped the canvas PAINTING and left the script DRAWING: rAF throttling keys off document visibility, not element visibility, so `visibility:hidden` never slowed the loop. `voiceOrbViz` is the only unconditional self-re-arming rAF loop in the shell after boot (every other is debounced one-shot or bounded; the splash lottie is explicitly destroyed). Fixed by handing the same verdict to script as well as CSS | 2026-09-07, defect measured on hardware; the CPU drop needs a page reload and is NOT yet measured there |
| Daemon goals produce work, and dispatch is never CALLED | settled on the box 2026-09-07. The dispatch logic is sound: invoking it directly through the MCP bridge dispatched all 6 queued tasks instantly (`{"dispatched": 6}`). So nothing is wrong with matching or delivery; `resource_governor._proactive_check_tasks` simply never calls it, gated `if self._mode != MODE_IDLE: return`. Its three conditions all read as idle from outside the process (no chat turn has bumped the activity timestamp, ext_cpu ~44% against 0.85, memory 30.3% against 0.90), so either the live mode disagrees or the proactive loop is not ticking that branch. The gate is the break; which half of it remains open | 2026-09-07, measured on hardware |
| The hive copilot FABRICATES completions and starves the real executor | reproduced live on the box 2026-09-07, the proof 7dc4da3 was waiting for. Drove the LIVE dispatcher through the MCP bridge (`dispatch_hive_tasks` -> `{"dispatched": 6}`), and all 6 tasks went straight from `pending` to `completed` with `quality_score: 0.5` and `spark_reward: 15` each, while `hart-copilot-daemon` kept logging `no task assigned by the hive`. The real executor never saw them. That fixed 0.50 is the in-backend `_dispatch_to_pipeline` signature: it asks the LLM for a diff, parses it, applies nothing, reports success, and awards sparks. `HEVOLVE_HIVE_INPROCESS_EXEC` is NOT set on this node, so it defaults to '1' and the fabricator wins the race on a node that runs the real daemon | 2026-09-07, measured on hardware |
| The node has been in ACTIVE mode since boot, and that starves everything | the single finding under most of the others, measured on the box 2026-09-07. `hart-nunba` logs every 30s: `Agent daemon: STARVATION OVERRIDE - yield gate has blocked on 'governor_throttle' for 12204s (>120s threshold); forcing one tick`. 12204s IS the whole boot. `_calculate_throttle` returns 0.05 in ACTIVE and up to 1.0 in IDLE, so a permanently-blocked yield gate means permanently ACTIVE. CONSEQUENCES, all previously logged as separate mysteries: `_proactive_check_tasks` returns early so hive tasks NEVER dispatch (the 6 pending); the agent daemon starves and forces a tick every 30s to survive; those forced ticks saturate a single-slot llama-server (10 inferences per 10 min at ~64s each, over 100% subscribed) which is why `/health` times out and `system_health` calls the LLM `down` when it is answering 200s; and llama at 2.5 cores plus the orb loop's core keep the box at 70.8% busy. OPEN: why it is ACTIVE. ext_cpu (~44%) and memory (30.3%) are both under threshold, so it must be `not user_idle`, which means something is calling `report_user_activity()` | 2026-09-07, measured on hardware |
| The agent engine runs in one process and is queried in another | settled on the box 2026-09-07. `:6777` is served by pid 1263 (`hart-backend`), while the agent daemon and NodeWatchdog live in pid 1268 (`hart-nunba`, running `nunba/main.py` behind `/run/hart/nunba.sock`). DIFFERENT PROCESSES. So `agent_status` over the MCP bridge on :6777 reads an `agent_daemon` module in a process that never started one, and its `daemon_enabled: false, daemon_tick_count: 0` says nothing about the real daemon; `_tool_impls` documents this as a module shadow, but here it is not even the same interpreter. The shadow-immune correction cannot rescue it either: `/api/agent-engine/ledger/stats` returns 404 on :6777 AND on the nunba socket, so the blueprint is registered on NEITHER. `list_routes` on :6777: 837 routes, zero agent-engine. Meanwhile the real daemon clearly IS alive in 1268, which is where the 30-second STARVATION OVERRIDE lines come from. Net: the agent engine is running and structurally unobservable | 2026-09-07, measured on hardware |
| A store-path overlay is TAMPERING, and the node acts on it | self-inflicted on the box 2026-09-07, recorded so nobody repeats it. My bind-mount of a patched `liquid_ui_service.py` over its store path tripped `runtime_monitor` at 13:52:03 (`TAMPERED FILE`), which does more than log: it calls `gossip.stop()` and halts itself. Gossip stopped in BOTH processes at 13:52:13/17, and hive federation stayed dead for over three hours (freshest peer contact frozen at 11749s, zero peers seen in 15 min). It did NOT recover when I unmounted; it took a restart of hart-backend and hart-nunba. After that: freshest 9s, 564 of 674 peers seen within 5 min. The `runtime_monitor` restart_count of 3 (14:42/15:37/16:32) is the watchdog restarting a monitor that kept re-detecting the overlay and halting again. PROVE WITH AN OVERLAY, THEN UNMOUNT IMMEDIATELY | 2026-09-07, measured on hardware |
| This session's nix work is BUILT into a real generation on the box | 2026-09-09: pushed 121 commits, CI built `iso-desktop` green and its "Push system closure to the fleet cache" step succeeded, then the node fetched that closure FROM the fleet cache (`dry-activate` resolved commit `760c32c7`, copied 38 paths from `etime.hertzai.com:8093`, zero errors) and `nixos-rebuild boot` staged it as generation 2 with `default nixos-generation-2.conf`. Inspecting the staged generation's OWN unit files proves every module change evaluated as intended, with no reboot required: `HART_COMP_SOCK=/run/hart/hart-comp.sock`, `NIX_PATH=nixpkgs=flake:nixpkgs`, `HEVOLVE_HIVE_INPROCESS_EXEC=0` (0 BECAUSE this node runs the copilot daemon, so the conditional resolved correctly), both relay socket units present, and the unit PATH now 50 entries carrying `/run/current-system/sw/bin` and `/run/wrappers/bin`. RUNTIME behaviour is still unproven: that needs the reboot | 2026-09-09, built and staged on hardware |
| EVERY fix from this session is running on hardware, delivered by OTA | 2026-09-09, the full loop closed: push -> CI -> fleet cache -> node fetch -> `nixos-rebuild boot` -> reboot into generation 2. Booted with `LoaderEntryOneShot=gen2` over `LoaderEntryDefault=gen1`, so a failed boot would have fallen back by itself. PROVEN RUNNING: Tier-1 hart-comp held through the upgrade; SSE stream-open **15.013s -> 0.016s** on the shipped build (not an overlay); `hart-comp-ipc.socket` and `hart-sway-ipc.socket` both `active`; `HEVOLVE_HIVE_INPROCESS_EXEC=0`; `NIX_PATH=nixpkgs=flake:nixpkgs`; the PATH floor in the live unit. And the deployment gap that could not be tested before: as **uid 992, the `hart` service user**, `HartWmClient` resolved `backend=hart-comp` via `/run/hart/hart-comp.sock`, switched workspace 2 then back to 1, and got `not_found` with the compositor's own message for a bogus handle. The brain drives the compositor through the relay | 2026-09-09, measured on hardware |
| The node suspended ITSELF because a worn battery confused a probe | root cause of the whole starvation story, found 2026-09-09 the moment the governor became observable. `/api/social/dashboard/health` reported `mode: sleep, throttle: 0.0, gpu_allowed: false, cpu_limit: 0.0` -- not ACTIVE as I had inferred for ten iterations. MODE_SLEEP has exactly one trigger, and the box is a laptop with a dead battery: mains plugged in (`ADP1/online 1`) but `BAT1` reading `Not charging` at 1%, so `psutil.sensors_battery()` returns `power_plugged=None`, and `not None` is True. The governor read a plugged-in machine as on-battery-and-critical and suspended everything. THAT is why the agent daemon logged a STARVATION OVERRIDE every 30s for its whole uptime, why 6 hive tasks were never dispatched, and why forced ticks kept a single-slot llama-server over 100% subscribed. Fixed: a definite psutil answer is trusted, `None` asks the kernel's `type=Mains`/`online`, and an unanswerable probe assumes mains, because guessing wrong toward battery suspends the node while guessing toward mains merely lets work run | 2026-09-09, measured on hardware |
| The copilot no longer fabricates, and its daemon actually runs | proven on hardware 2026-09-09 AFTER the OTA carried `HEVOLVE_HIVE_INPROCESS_EXEC=0`. Same MCP dispatch that produced 6 instantly-`completed` tasks at `quality_score 0.5` on the old build now leaves them for the real executor: `{"dispatched": 3}` -> **3 pending, 0 completed, 0 fabricated**. And `hart-copilot-daemon`, which had logged `idle / no task assigned by the hive` for its entire previous uptime, now logs `yield gate OPEN (was model_pressure)` and `"action": "ran"`. It resolves its backend correctly (`http://localhost:6777`) and can see all 3 pending. It still cannot COMPLETE work: no Anthropic credential is provisioned, so `claude -p` hangs and the run reports ok=false. That is a provisioning decision, not a defect | 2026-09-09, measured on hardware |
| The cross-OS runtime smoke test TIMES OUT on the box | 2026-09-09, found while checking failed units after a GC. `hart-compat-smoketest.service` -- the live runtime proof behind the Windows/macOS parity claim -- fails with `result 'timeout'`, SIGKILLing winedevice.exe after 1min 54s of CPU, and leaves `/run/hart/compat-status` EMPTY. So parity is green in the 30-test matrix (which checks capability rows against the tree) while its runtime probe never completes on hardware. Plausibly downstream of the SLEEP-mode starvation, since the node is carrying llama at ~2.5 cores with the governor holding throttle at 0.0; the battery fix in 36432de is the thing to re-test this against | 2026-09-09, measured on hardware |
| ONE boolean was starving the whole node, and fixing it is validated on hardware | generation 3 (2026-09-09) carries the battery fix and the agent-engine registration, OTA'd the same way as gen 2 and booted with `LoaderEntryOneShot=gen3` over a gen-2 fallback. GOVERNOR: `mode sleep -> idle`, `throttle 0.0 -> 1.0`, `cpu_limit 0.0 -> 0.8`, and it now cycles idle/active normally instead of being pinned. DOWNSTREAM, all measured: STARVATION OVERRIDE lines went from one every 30s for the entire 12204s uptime to **13**, early-boot only; LLM inferences per 10 min 10 -> 4; and llama `/health`, which TIMED OUT at 30s before, now answers **200 in 0.0013s** (it was never down, only saturated by the forced-tick storm); `hart-compat-smoketest` no longer fails, and the node reports ZERO failed units; total CPU 0.437 -> 0.161. OBSERVABILITY: `/api/agent-engine/ledger/stats` answers **200** instead of 404 on both processes, and gives the definitive daemon answer at last -- `stats: 0 tasks, 0 sessions`, so daemon goals are not broken, there are simply NONE DEFINED on this node | 2026-09-09, measured on hardware |
| Windows apps really do run on the hart node, and the OS can finally say so | `hart-compat-smoketest` exists to turn the foreign-OS support CLAIM into a MEASUREMENT, and on hardware it was doing neither: SIGKILLed twice with `Failed with result timeout`, `/run/hart/compat-status` EMPTY, zero verdict lines. Two defects, each measured first. (a) Probes captured with `OUT="$(timeout N ...)"`; a substitution waits for EOF, and Wine's wineserver/winedevice.exe inherit the pipe's write end and outlive `wine`, so the bound stopped bounding. Back to back, same prefix: **via `$(...)` 41s, via a file 20s**, for a `timeout 20`. (b) `TimeoutStartSec` was a hand-written **360** against inner bounds summing to **390**, so a run where every probe merely hit its own limit could not finish inside the unit's. Fixed: one file-capturing `probe` helper, and the ceiling DERIVED from a budget attrset. RESULT ON HARDWARE, rendered script as user hart: **SIGKILL at 360s with 0 verdicts becomes rc=0 in 65s with 11** -- `windows=ok`, `linux=ok`, `flatpak=ok`, `appimage=ok`, `fs_ntfs/exfat/vfat/ext4/btrfs=ok`, `android=no-image` (AOSP not downloaded), `macos=skip` (darling opt-in, absent). Wine was never broken; the unit died before it could ask | 2026-09-10, measured on hardware |
| Three animations were eating three quarters of a core on the shell, and the gate meant to stop them was losing the cascade | The shell's WebKit main thread sat at **98% of a core continuously from boot** while `hart-comp` idled at 2.2%. I reached two wrong conclusions on the way and both are worth recording. FIRST I blamed the ungated onboarding orb without testing it. THEN, seeing the thread yield only **3 times a second** (voluntary +37 vs nonvoluntary +173 over 10s), I called it a synchronous busy loop and said it was NOT an animation. Also wrong: 3 yields/sec is equally **~3 frames per second at ~330ms each**, which is exactly what re-rasterising large blurs in software costs, and it explains the idle compositor too (only 3 frames to composite). What settled it was a real experiment: a SECOND WebKit on the same URL reproduced 98% within 18s of a cold load, giving a sandbox that never touched the live shell, and injected USER stylesheets then varied one rule at a time on the real origin. `*{animation:none}` took **98% -> 19%**. `document.getAnimations()` named all six: three correctly `paused` by the gate (hhLiveDot, vBlob1, vHue) and three `running` -- `tbOrbBreathe` on `.top-bar-orb`, `hob-breathe` on `.hob-orb`, `lg-pulse` on `.hart-senses-btn.is-sensing .mi`. Costs are NON-ADDITIVE (with any one still running the frame clock ticks and everything repaints): top-bar-orb alone 98%, +hob-orb **55%**, +lg-pulse **22%**, against 21% for all-off. THE DEFECT for `.top-bar-orb`: `hartHome.css:734` says `body.gpu-hardware .top-bar-orb{animation:tbOrbBreathe...}` and the gate says `body.webkit-flat .top-bar-orb{animation:none}` -- an exact specificity TIE at (0,2,1), the body carries BOTH classes, so SOURCE ORDER decides and the external sheet wins. The gate had been silently losing since it was written. Fixed with `!important` (a hardware floor, not a style preference) plus the missing `.hob-orb`. MEASURED effect of exactly what the fix disables: **98% -> 55%**. `lg-pulse` is the remaining 33 points and is NOT fixed here: suppressing it removes a real user signal, and its running at idle may mean `is-sensing` is stuck, which is a different bug | 2026-09-10, measured on hardware |
| Two hive task queues on one machine, and four of six hart units were writing to the orphaned one | `get_data_dir()` resolves NUNBA_DATA_DIR, then HARTOS_DATA_DIR, then a probe for `/etc/hartos-release`, then the Linux default `~/.config/nunba`. Only `hart-backend` and `hart-agent` set HARTOS_DATA_DIR, so `hart-nunba`, `hart-copilot-daemon`, `hart-liquid-ui` and `hart-model-bus` fell through to the default. Same machine, same user, same HOME, two dispatcher queues: `/var/lib/hart/agent_data/hive_tasks.json` (11 tasks, **assigned**, mtime 23:37, live) and `/var/lib/hart/.config/nunba/agent_data/hive_tasks.json` (9 tasks, **pending**, mtime 23:17, written once at boot and orphaned). Those nine can never dispatch: the session registry lives in the process reading the other file. Nothing errors, nothing logs. `/etc/hartos-release` does not exist so the embedded-OS branch never fires either, and it must not, because it resolves to `/var/lib/hartos`, a THIRD root. Fixed with ONE `systemd.globalEnvironment` declaration in the module that owns `hart.dataDir`, which also covers units added later; copying it per-unit is precisely how the split happened | 2026-09-10, measured on hardware |
| Hive auto-dispatch works, and the proof was sitting in the queue the whole time | Earlier I created a hive task, watched 240s, saw no dispatch, and concluded the battery fix was necessary but not sufficient. Wrong: `TASK_CHECK_INTERVAL` is 600s with +/-50% jitter, re-armed on every non-IDLE iteration, so the floor between reaching IDLE and a dispatch is **300s**. The window was below the floor. An hour later `/api/hive/session/tasks` shows that exact task delivered to session `chs_b82d2a403c7e`, described `"created, never manually dispatched"`, alongside a real benchmark shard. Dispatcher side: 11 tasks at `assigned`. Copilot daemon is running and cutting branches, but has **no Anthropic credential** (0 such vars in its environment though the `claude` CLI is present), so it polls, claims, and cannot complete. That last part is an owner provisioning decision, not a defect | 2026-09-10, measured on hardware |
| Windows apps run on the hart node, proven with real PE binaries and not just an echo | `windows=ok` in the smoke test only proves `cmd /c echo`. On a clean prefix: `wineboot --init` rc=0, `cmd /c echo HARTOK` rc=0, and **`cmd /c ver` rc=0 returning `Microsoft Windows 10.0.19043`** -- a genuine Win32 PE executing and reporting the emulated version -- plus `attrib.exe` rc=0, with 93 PE binaries in the prefix. An earlier run of the same commands timed out with `process_send_command receiving command result timed out` and a NULL page fault; that was MY doing, from `pkill -9 wineserver` during debugging, which wedges a prefix. The graceful `wineserver -k` used by the smoke-test fix does not | 2026-09-10, measured on hardware |
| The Nunba microfrontend serves end to end, and my first reading of it was wrong | I probed `/api/nunba/` on :6777, got 404 with `Server: waitress`, and called it a regression. That was never the contract: Nunba is served at the ORIGIN ROOT of :6800 as a no-basename history-router SPA. Tested correctly: `/cors/test` 200 `ok`, `/social` 200 with 20680 bytes and `<title>Hevolve AI</title>` plus `id="root"`, `/dashboard` 200 returning the same index (history fallback behaving), and `Server: hypercorn-h11` on those paths, i.e. proxied through to the Nunba app over its socket | 2026-09-10, measured on hardware |
| OS apps are installed and reach the desktop; macOS is the one parity leg that is NOT proven | Live on the node: `/api/shell/apps` answers **200** with the desktop's list (Waydroid, Audacity, LibreOffice Base, btop, dconf-editor...), the catalog holds **34** entries (Firefox, GNOME Web, Chromium, Celluloid, VLC...), and the system carries **161 desktop entries** and **1909 launchable binaries**, with firefox, libreoffice, gimp, vlc and chromium all present and executable. App LAUNCH verification stays out of scope by owner direction (C1). THE HONEST GAP: `macos=skip` because darling is not installed. Windows is proven with real PE binaries and Linux/Flatpak/AppImage plus five filesystems are `ok`, so "full parity with windows and macos" is currently **windows yes, macos not demonstrated on this node** | 2026-09-10, measured on hardware |
| An agent recipe drove a real window through the compositor, start to finish | Not an endpoint check: brain -> IPC -> compositor -> a REAL Wayland toplevel -> verified state change -> cleanup. `window.list` **0 toplevels**; launched a GTK4 toplevel, which appeared as `win_1b` at 1280x800 (476,356); `window.focus` **ok, focused:true**; `window.move` to (120,80) **ok**; `window.resize` to 900x600 **ok**; `window.list` then reports geometry **{x:120, y:80, w:900, h:600}**, i.e. the compositor really applied both; `window.close` **ok, closed:true**; `window.list` back to **0**. MINOR API DEFECT FOUND: `window.resize`'s RESPONSE echoes the PRE-resize geometry (1280x800) while the subsequent list shows the real 900x600, so a recipe that trusts the response would conclude the resize failed. The move response does not have this problem | 2026-09-10, measured on hardware |
| Liquid UI verified by running it, not by reading status codes | Injected script into the live shell in a real WebKit: **topbar=1**, **desktopIcons=9** rendered, and all five shell globals present (`HartHome`, `HartPalette`, `hartInstallIcon`, `HART_NATIVE_CHROME`, `HART_PERF`), which proves the modules EXECUTED rather than merely being fetched with a 200. A synthetic keydown was delivered to the page (`keydownDelivered=true`), so the shell's event loop is live. It also confirmed independently that **`onboardingOpen=true`** -- the overlay carrying the `hob-orb` animation really is up on this node, corroborating the rendering work. INCONCLUSIVE, not a defect claim: a click on the first `.hart-onboarding button` did not dismiss the overlay, but that selector may not be the skip control; `dock=0`/`taskbar=0` likewise reflect my selectors not matching the shell's class names rather than missing chrome | 2026-09-10, measured on hardware |
| A leftover bind mount into /nix/store had silently disabled garbage collection, and the next OTA would have failed on it | Checking headroom before the OTA found root at **99%, 523 MiB free** on the 28G stick, down from 932 MiB earlier the same night. `nix-collect-garbage` was reporting `0.00 MiB freed` every run, aborting on `cannot unlink .../hart-comp-session: Device or resource busy`. Not a running process: `/proc/mounts` showed a BIND MOUNT over that store file, from the temporary socket-wait override `ExecStartPre=-mount --bind /var/lib/hart/hart-comp-session-socketwait60 /nix/store/<gen2>-hart-comp-session/bin/hart-comp-session`, re-applied at every greetd start. Two compounding faults: it pinned a GENERATION-2 path so it had stopped doing anything functional once gen 3 booted, and you cannot unlink a mountpoint, so **GC aborts the entire run on the first failure** and the node could never reclaim anything. One `umount` and a single GC freed **298 MiB**, taking the node to **94%, 1.9G free**. The override is now removed (`/etc/systemd/system.control` empty and deleted, daemon-reload done, greetd still active, zero store mounts); gen 3's own session script already carries the same `seq 1 300` wait, so it was redundant as well as harmful. RECOMMENDED, deliberately NOT done blind tonight: a free-space preflight in `hart-ota-apply` before the switch. `nixos/tests/ota-central.nix` drives that engine with a stubbed rebuild and sets no `diskSize`, so a threshold added without updating that test would turn a green CI leg red | 2026-09-10, measured on hardware |
| The input half of input-to-photon is now proven on hardware; the photon half still has no number | The node had never been touched: `input-alive` ABSENT, **0** `hart-latency` lines, no `latency.jsonl`, `0 users`. The instrument was not broken, it had never been given an event, and an untouched machine looks exactly like a healthy instrument from a distance. Manufactured real input via `/dev/uinput` (raw ioctls; `python-evdev` is absent), deliberately at the KERNEL layer because `T_input` is the kernel timestamp on `input_event` and anything injected higher measures a different, flattering thing. The compositor adopted it unaided: `smithay::backend::libinput: New device "event10"`, then **`hart-comp: first seat input delivered -- libinput/Seat path is LIVE (#134 liveness beacon)`**, and `input-alive` went absent -> PRESENT. STILL OPEN: ~100s of hover/scroll/click driven together with **337** `window.move` calls to force damage took hart-comp from 2% to 13% CPU, so it was compositing, and produced **0** `hart-latency` lines. Ruled out by inspection: the instrument IS in the real binary (the `/run/wrappers` path is a setuid shim with no strings, which invites the opposite conclusion), both `on_frame_queued` (`udev.rs:1714`) and `on_frame_presented` (`udev.rs:1214`) are wired, compositor `info!` does reach the journal, and `HART_LATENCY_JSONL` gates only the jsonl sink. The narrow open question is the window aggregation in `latency.rs` | 2026-09-10, measured on hardware |
| macOS APPS RUN ON THIS NODE. I reported the opposite twice before testing it properly | **`darling shell echo HARTOK` -> rc=0 after 155s, HARTOK returned.** A Darwin command really executed on the Samsung. Both earlier claims were wrong. (1) "darling needs the `darling-mach` KERNEL MODULE which nixpkgs lacks": the module is indeed absent, but darling 2024-02-03 ships **`darlingserver`**, the userspace architecture that REPLACED the LKM, so `/dev/mach` is not expected to exist. I made a coincidence into a cause. (2) "it hangs at prefix setup": it hung because that run used the default `/root/.darling`, left half-created by an earlier run I had killed -- the SAME wedged-prefix pattern I had already hit with wine and should have recognised. A FRESH prefix completes in 155s. CONSEQUENCES: `hart.subsystems.macos.enable` would deliver working macOS support on this hardware, so the assertion message I wrote about kernel modules is doubly wrong and needs replacing; and the smoke test's `darling = 90` budget cannot cover a 155s cold start, exactly like the wine budget it sits beside. Both are `nixos/` edits held back deliberately while a build is in flight, since that path cancels it. WINDOWS + macOS PARITY IS THEREFORE REACHABLE ON THIS NODE, which is the opposite of what I reported twice | 2026-09-10, measured on hardware |
| The node still cannot self-build an OTA, re-measured on the current tree rather than taken on faith | hart-ota.nix's header says nodes must not build, from a 2026-08 measurement ("352.94 MiB download, then 153 derivations"). That predates the fleet cache, so it was worth re-testing before waiting hours on CI: the node now HAS 676beda's closure installed and the cache is reachable, so perhaps only the delta would build. `nixos-rebuild dry-build --flake 'github:hertz-ai/HARTOS?dir=nixos#hart-desktop-raw'` fetched f497d45 and answered exactly: **677 derivations will be BUILT** and **312 paths fetched (783.85 MiB download, 3768.64 MiB unpacked)** -- the build list including five `stdenv-linux`, gcc, binutils and a musl cross toolchain. Against **1.3G free**, the FETCHES ALONE need three times the space before a single build starts. The cache cannot help because it does not hold HEAD's closure; that is precisely what CI is building. So the module's rule stands, now with current numbers: CI is the only builder, and there is no shortcut. The dry run itself pulled 600 MiB while evaluating and was killed and garbage-collected back to 1.9G, which is its own small lesson -- a `--dry-run` on Nix still downloads | 2026-09-10, measured on hardware |
| OTA IS PROVEN END TO END ON HARDWARE: push -> CI -> fleet cache -> fetch -> stage -> boot | The half that had never been demonstrated. CI's `iso-desktop` leg finished and pushed **3260 narinfos** to the fleet cache. Pinned to that exact rev (`github:hertz-ai/HARTOS/e50733b...?dir=nixos#hart-desktop-raw`, because HEAD would have needed 677 local builds), `nixos-rebuild boot` created **generation 4** with no errors, both boot entries present. Safety first: persistent default left on the KNOWN-GOOD generation 3 with a **one-shot** EFI var into 4, so a failed boot returns to 3 unattended rather than needing physical access. Rebooted, and the box came back in ~120s reading **`/etc/hart/image-rev = e50733b`** with `booted == current`. ZERO REGRESSION on the far side: **no failed units at all** (was 1), shell/nunba-spa/cors/ledger/hive all **200**, governor still `mode=idle throttle=0.8` | 2026-09-10, measured on hardware |
| The cross-OS smoke test completes under systemd now, and its first honest run found a bug in my own budget | Before: `failed`, `Result=timeout`, `ExecMainStatus=15` (SIGKILL), status file EMPTY. After the OTA: **`Result=success`, `ExecMainStatus=0`, 13 verdicts** written by the UNIT rather than by me running the script by hand. It also reported `windows=failed`, and the diagnostic line added with the fix said exactly why: `probe exceeded its bound of 90 s: wine cmd /c echo HARTOK`. That verdict was an artifact of MY tuning, not of Wine -- a separate clean-prefix run on the same box needed up to **240s** for `wineboot --init` alone, and a freshly-booted node has a cold prefix. Budget raised 90 -> 180, which is only safe because TimeoutStartSec is DERIVED now (sum 380 -> ceiling 530 automatically); before the derivation, this exact edit would have re-created the original bug | 2026-09-10, measured on hardware |
| The cascade-tie diagnosis was tested rather than asserted, and it held | `e50733b` contains the onboarding-orb SELECTOR (from 9582a51) but NOT the `!important` that makes the gate win (61189a0, a later commit). If the tie theory were wrong, the selector alone would have quieted the orb. On the booted generation: **`onboarding orb in the gate: 1`**, **`animation:none!important served: 0`**, **shell CPU still 91%**. The rule is present, matches the element, and loses anyway -- which is what an exact specificity tie decided by source order looks like. The fix that wins the tie ships in the next closure | 2026-09-10, measured on hardware |
| Every subsystem re-verified ON the OTA-deployed generation, not just before it | Proving a fix and proving the DEPLOYED system are different claims, so all of it was re-run on generation 4 (`image-rev e50733b`) after the reboot. **AGENT RECIPE** end to end: `window.list` 0 toplevels -> launched `win_1` -> `focus` ok -> `move` ok -> `resize` ok -> list confirms **{x:120, y:80, w:900, h:600}** -> `close` ok -> back to 0. **LIQUID UI** executed, not pinged: `topbar=1`, `desktopIcons=9`, all five globals present (`HartHome`, `HartPalette`, `hartInstallIcon`, `HART_NATIVE_CHROME`, `HART_PERF`, so the modules RAN), `keydownDelivered=true`. **NUNBA**: `/cors/test` 200, `/social` and `/dashboard` 200 at 20680 bytes, **`Server: hypercorn-h11`** (proxied to the app, not the shell) and `<title>Hevolve AI</title>`. **HIVE**: a session reconnected after the reboot (`chs_a115ffa007c4`, idle). **WINDOWS**: real PE binaries, `cmd /c echo HARTOK` rc=0 with **HARTOK returned**, `cmd /c ver` rc=0 returning **`Microsoft Windows 10.0.19043`** -- so the unit's `windows=failed` really was only the 90s budget. **COMPAT UNIT**: `Result=success`, 13 verdicts. **ZERO FAILED UNITS**, governor `mode=idle throttle=0.8` | 2026-09-10, measured on hardware |
| A real OS app launches and is driveable on the deployed generation, and `window.resize` lies about it | On generation 4: `window.list` **0 toplevels** -> launched **gnome-calculator**, a genuinely installed application (161 desktop entries, 1909 binaries on this node) -> it appeared as **`win_3` titled `'Calculator'`** at 360x626 -> `window.focus` **ok** -> `window.move` **ok**, geometry really moved to (200,140) -> `window.close` **ok** -> back to **0**. Launched with the session's own uid and environment, so this is the app path a user takes, not a side door. SECOND API DEFECT, and a sharper case than the first: `window.resize` to 700x500 returned **`ok: true`** and the geometry stayed **360x626 before AND after**. The client simply declined, which is correct Wayland (a configure is a request, and Calculator has size constraints that my WebKit test window did not), but the compositor reports SUCCESS for a resize that never happened. Combined with the earlier finding that resize's response echoes PRE-resize geometry, a recipe author is misled twice: the response says ok, and the geometry it quotes is stale. `window.move` is honest in both respects | 2026-09-10, measured on hardware |
| Daemon goals are not broken: there are none defined, confirmed at the table the daemon actually reads | I answered this once from `/api/agent-engine/ledger/stats` ("0 tasks, 0 sessions") and that reading was worthless: the endpoint reports `source: flask_in_process` with `running: false, tick_count: 0`, i.e. the backend's OWN in-process daemon, which was never started in that process. Right answer, wrong instrument. The daemon queries `AgentGoal.status.in_(['active','completed'])` against `agent_goals` in `HEVOLVE_DB_PATH`, so that is the source of truth. On the node: **`agent_goals` 0 rows, `coding_goals` 0 rows.** Tables present, schema correct, daemon wired to them, and nothing in them. So the goal machinery is intact and simply unseeded, which is a product decision and not a defect -- and it also explains the daemon having nothing to do while it was starving on `model_pressure` | 2026-09-10, measured on hardware |
| ROOT CAUSE FOUND: the latency instrument can never emit, because its two clocks have different EPOCHS | Why `hart-latency` has produced zero lines on every node, ever. `event_us` is libinput's CLOCK_MONOTONIC **since boot**. `instant_us()` is `base.elapsed()` where `base = Instant::now()` **at compositor start**. Same clock, different ORIGIN. For one moment `t`: `event_us = t - boot` and `instant_us = t - comp_start`, and since `comp_start > boot`, **`instant_us` is always the SMALLER number**. `note_input` records its offset observation only `if instant_us >= event_us`, which is therefore never true, so `offset_obs` stays empty and `offset_us()` returns `None` forever. `frame_presented` then hits `if let Some(off)` and records nothing -- the anti-gaming rule ("report nothing rather than something almost right") firing on every sample rather than on bad ones. The module doc's premise, "an event can only be observed AFTER the kernel stamped it", is true WITHIN one epoch and is swamped by the boot-to-compositor gap ACROSS two. THE FIX has three parts, because the sign inverts twice: observe `delta = event_us - instant_us` (a positive constant, `comp_start - boot`, minus the delivery delay); take the rolling **MAXIMUM**, since a larger delivery delay now SHRINKS the delta, so convergence is from BELOW; and compute `photon_event_us = instant_us + off` rather than minus. The 22 unit tests pass because they hand the core a same-epoch pairing, so they encode the assumption instead of testing it | 2026-09-10, found by reading against hardware behaviour |
| A CI leg has been red for a week, and the cause is crates.io blocking curl, not our code | `Build hart-rust-precedent` has FAILED on every matrix run since **2026-09-03** and never once succeeded. The job log names it exactly: `curl: (22) The requested URL returned error: 403` ... `cannot download crate-wasm-bindgen-macro-support-0.2.116.tar.gz from any mirror` -> `cargo-vendor-dir.drv` fails -> `hart-claw-precedent-0.1.0.drv` fails. The crate is fine: `static.crates.io` serves that exact tarball **200**. It is the endpoint Nix fetches from, tested directly: `crates.io/api/v1/crates/<crate>/<ver>/download` returns **403 with no User-Agent, 403 with `curl/8.12.1`, and 200 with `Mozilla/5.0`** -- and `serde 1.0.228` behaves identically, so it is not crate-specific. crates.io has added bot protection that rejects curl-style agents, and Nix's crate fetcher is curl. That also explains why `Build hart-comp` SUCCEEDS in the same run while this fails: hart-comp builds from the offline-vendored lock, and only the precedent vendors from crates.io at build time. Not our regression, but it is our permanently-red gate, and a gate that is always red catches nothing -- the same shape as the `Nix Lint & Evaluate` leg. THE FIX, written down rather than guessed at: the precedent uses `cargoLock.lockFile`, so nixpkgs' `importCargoLock` fetches every crate via `fetchurl` from that exact 403ing URL. The candidates are (a) a `fetchurl` overlay adding `curlOptsList = ["--user-agent" "Mozilla/5.0"]` -- safe for fixed-output hashes, since a UA does not change content, but it touches EVERY fetch in the tree; (b) redirect crate fetches to `static.crates.io`, which serves the same tarballs at 200; or (c) vendor offline the way hart-comp already does, which is why hart-comp survives. NOT attempted blind: deepbox has no nix and the node cannot build, so there is nowhere to test an overlay that could turn more legs red than it fixes | 2026-09-10, measured |
| The split data root had minted the node TWO IDENTITIES, and the fix is confirmed to have closed it | Auditing for further duplicate paths after fixing the queue split, and this is the sharpest evidence of what a parallel path costs. Files present in BOTH roots: `agent_data/hive_tasks.json`, `node_id.json`, `device_id.json`, `guest_id.json`. Comparing contents: **`node_id` live `cd5dcb76-8dba-491a-b15e-3820ed4f1575` vs old `b9afded7-9a07-4fc9-b2ea-d570bc967b54`** and **`device_id` `e73b8359...` vs `0ecf57df...`** -- DIFFERENT, because both are generated randomly on first use and each root minted its own. `guest_id` is IDENTICAL in both, and only because it is derived from `linux_machine_id` rather than random, so it could not diverge. The hive federates by node id, so one machine could present as two peers. THE FIX IS CONFIRMED WORKING: nothing has been written to the old root since the generation-7 boot, while the live root takes every write (`logs-dev/langchain.log` at 10:50, current). `get_log_dir()` routes through `get_data_dir()` so it followed automatically, and `systemd.globalEnvironment` covers units added later. LEFT ALONE deliberately: 24M of stale data under the old root, including 9 orphaned hive tasks and the conflicting identity. It is inert but it is real data, so discarding it is an owner call, not a cleanup | 2026-09-10, measured on hardware |
| The latency instrument is PROVEN working on hardware, and its per-component attribution is PROVEN dead | Two findings from one session. FIRST, the instrument reports real input-to-photon numbers, which it had never been seen to do. Steady state under human-cadence hover: `p50=11.2ms p99=34.7ms max=35.5ms budget=16ms`, with `max` almost equal to `p99`, so the distribution is tight and 34.7ms is close to two vblanks at 60Hz, which is what a client repaint round trip costs by construction. Under a synthetic 250 events/sec storm the tail inflated to `p99=653ms`, so ALL earlier storm numbers overstate it: many inputs stack onto one flip and the oldest carries the whole gap. Zero drop records and zero stall records throughout, which is the instrument's own statement that the numbers are trustworthy. SECOND, and against that: ~4,200 samples swept over the entire output, six passes through the top-bar band, a hard dwell on the right edge where ORB_SM sits, and 100% came back `component=shell`. Nine windows, one distinct component. Every per-component row in latency_budgets.json is therefore dead on this node and the native-versus-shell delta cannot be measured at all. NOT a guess at the cause: the layout is pinned correct by component_at tests (scene.rs:4394-4487, top bar / omnibox / taskbar / cards / hero orb / compact orb), the DRM backend does override native_tree (wayland.rs:463) and udev.rs runs on crate::wayland::State, and lower_scene calls tree_for unconditionally since that is what publishes the chrome mask the node reports as `bloom,orb`. So the fault is runtime, and pointer_surface now names which of its four fallback branches fired, with coordinates | 2026-09-10, measured on hardware |
| Three latency bugs were real; the evidence used to hunt them was not | The journal was read as `journalctl -u hart-comp` through three consecutive rounds of diagnosis. There is NO hart-comp.service. The compositor runs in greetd's `session-1.scope` under the identifier `hart-tier-hart-comp`, so that query matched nothing and printed `-- No entries --` rather than erroring, which is indistinguishable from a healthy quiet compositor. Read by PID or identifier instead, the instrument had been reporting all along. The three fixes stand on their own evidence and all three were genuine: the clock-epoch inversion is proven by a test that fails outright against the old code, the 600-vblank stall threshold could not be reached on a damage-tracked desktop, and take_stall was reachable only from the vblank handler whose absence it existed to report. Both things are true at once, and the cheap guard is to prove a journal query returns SOMETHING before concluding a behaviour is absent | 2026-09-10, measured on hardware |
| The hive queue has 18 tasks and NOTHING has ever reached a terminal state | Live on the node, generation 7. `agent_data/hive_tasks.json`: 18 tasks, 16 `benchmark` and 2 `code_write`, and every single one still reads `status='assigned'`. No task is `validated`, `failed`, `completed` or `cancelled`, and no quality value exists anywhere in the file. SIX of them finished their work on 09-07 16:37: `result` and `completed_at` are both populated, `error` is None. Their status never advanced, so three days later they are permanently in flight -- never validated, never failed, never paid Spark, and never removed from the queue. The 2 `code_write` tasks have no `completed_at` at all, so the actual coding work has never run once. WHAT IS NOT ESTABLISHED: the cause. Two candidate theories were checked and BOTH are dead. The benchmark result shape is legitimate, not cross-wiring: `hive_task_protocol` states for BENCHMARK that "the answer itself is the evidence: output, or test output", so `changes_applied=False` with a 16-char output passes the gate by design. And `validate_result` cannot return None, so the `quality >= 0.4` comparison in `on_task_result` cannot raise between the `completed_at` write and the status write. WHAT IS CODE FACT: there are TWO independent quality scorers for one task, `claude_hive_session._compute_quality_score` for the PeerLink report and `hive_task_protocol.validate_result` for status and Spark, and the dispatcher call that advances status sits under a bare `except Exception` logging at DEBUG, below the default info filter, so any failure there is invisible. Reported rather than fixed: copilot and hive are owner-gated lanes | 2026-09-10, measured on hardware |
| The per-component attribution is not broken, and the branch can be named without deploying anything | Yesterday's row called the attribution PROVEN dead and concluded that the fault was runtime, with the next boot's journal to say which half. It is not a runtime fault. `pointer_surface` falls back to `component=shell` in FOUR situations and the first one is `native_shell_on == false`. The DRM backend sets that field in exactly one place, `udev.rs:512`, `std::env::var_os("HART_NATIVE_SHELL").is_some()`. Read straight off the running compositor on generation 7 (pid 1871, `/proc/1871/environ` under sudo): the session carries `HART_SHELL_READY_FLAG`, `HART_COMP_PREFER_HARDWARE`, `HART_COPILOT_BACKEND`, `HART_INPUT_ALIVE_FLAG`, `HART_COMP_NO_TEST_CLIENT` and `HART_APP_CATALOG`, and **no `HART_NATIVE_SHELL`**. Nothing in `nixos/` sets it either, so the `hart.comp.nativeShell` option the program doc names is unbuilt. Corroborated from the other side: the compositor published **`chrome=bloom,orb`**, which is the flag-OFF legacy chrome block at `comp_core.rs:3352` (`if !capture_blocked && !native_shell_on`), not the native scene. So all ~4,200 samples were HONEST. On this node every pixel really is the WebView shell, and **`p50=11.2ms p99=34.7ms`** is the WebView baseline the native flip will be judged against, correctly labelled. What is dead is not the instrument but the COMPARISON, and no amount of hovering can revive it: the flag is read once, from the environment, at session construction | 2026-09-10, measured on hardware |

| The CI leg that had been red for a week is GREEN, and the first fix for it was only half the bug | `Build hart-rust-precedent` had failed on every run since 2026-09-03. The crates.io 403 was diagnosed and fixed yesterday (`f27f0ce`, download host moved to static.crates.io through `extraRegistries`), and the leg stayed red. The log says why, one step further on than the fetch: **`error: source https://github.com/rust-lang/crates.io-index defines source registry crates-io, but that source is already defined by crates-io`**. Read against `import-cargo-lock.nix` at the pinned nixpkgs rev, that is structural: the generated vendor `config.toml` always opens with `[source.crates-io]`, then appends a `[source."<url>"] registry = "<url>"` block per `extraRegistries` key, so keying it on the crates.io index defines one registry twice. The registry map is a `let` binding in that file, so `extraRegistries` is the only public way to reach the download URL and there is no way to reach it without also emitting the block that breaks the build. Fixed one layer lower, at `fetchurl`, where nothing is written into `config.toml`: an overlay on the 25.05 Rust instance rewrites URLs under `https://crates.io/api/v1/crates/` to `https://static.crates.io/crates/` and leaves every other fetch's arguments, and therefore its derivation, untouched. **Dispatched build on `cc8e5d3`: `Build hart-rust-precedent` SUCCESS**, 116 derivations built, and the log shows the exact crates the old failure named now downloading (`wasm-bindgen-macro-support 0.2.116`, `js-sys 0.3.93`, `getopts 0.2.24`) from static.crates.io. The guard that pinned the old spelling was re-pointed at the mechanism and mutation-checked by deleting the `overlays = [ ... ]` line and watching it go red | 2026-09-10, measured in CI |

| PER-COMPONENT latency attribution is ALIVE, and the first per-surface numbers on this program exist | Generation 8 (`image-rev 4e09e01`) carries both the attribution diagnostic and the new `shell.native` verb, and the first thing the instrument did on that boot was name its own fault: **`hart-latency attribution: falling back to component=shell` `reason="native_shell_on is false, so there is no native scene to attribute to"`**. That is branch one of four, which settles the hunt for a runtime bug that was never there. Then both halves were swept over the same five points (top bar, compact orb, card, hero orb, taskbar) at human cadence, minutes apart, on one boot. **A, scene off: three windows, ALL `component=shell`** (`n=284 p50=14.6ms p99=58.8ms max=220.0ms`, `n=240 p50=14.8 p99=59.0 max=225.5`, `n=176 p50=13.3 p99=59.0 max=218.4`). **B, after `shell.native {on:true}`: fourteen lines naming SEVEN components** where every previous measurement in this program's history had produced one. `top-bar p50=25.4 p99=46.4`, `omnibox p50=18.0 p99=33.3`, `orb p50=24.0 p99=46.9` and `p50=25.8 p99=41.5`, `home-row p50=22.0` and `p50=28.8`, `home-card p50=31.1 p99=48.8`, `taskbar p50=25.4 p99=38.8` and `p50=26.0 p99=36.2`, `shell p50=25.0..26.8 p99=43.3..50.5`. The flag went back off in the same run and reads `native = False`; the compositor is the same pid it was before the flip (1784, never restarted), zero failed units, and **zero drop records and zero stall records all boot**, which is the instrument's own statement that the numbers are trustworthy. Every row is `verdict=FAIL` against the 16ms budget, and that is the honest headline: nothing on this desktop meets its budget yet | 2026-09-11, measured on hardware |
| The 220ms tail every measurement has carried is the IDLE HEARTBEAT, not a stall | The A half's `max` is 218.4, 220.0 and 225.5ms across three consecutive windows, which is too tight a cluster to be jitter. `main.rs:586` says what it is: **`const IDLE_HEARTBEAT: Duration = Duration::from_millis(200)`**, the 5Hz floor a damage-tracked desktop coasts on when nothing is animating. An input that lands just after a repaint waits a whole heartbeat plus a frame, and 200ms plus one 16.7ms frame is 216.7ms. The B half is the control: with the native scene drawn the desktop animates by construction (`scene_animates` is gated on the flag, because the orb breathes off the clock), the compositor stops coasting, and **max collapses from ~220ms to 40-66ms** in every window. So the worst case on the shipped WebView desktop is a scheduling floor rather than a fault, and it is not fixed by making anything faster | 2026-09-11, measured on hardware |
| What the A/B does NOT show, stated because the numbers invite the wrong reading | B's p50 is WORSE than A's, ~25ms against ~14ms, and that is not evidence that native rendering is slower. `shell.native` does not ask the WebView shell to stand down, which is one of M6's four named obligations and a contract decision rather than a toggle. So B measured the native scene IN ADDITION TO the WebView, both compositing, on a node already at 94-95C under llama-server and WebKit with a load average of 5.3. A desktop drawing two homes at once costing more per frame than one is the expected result, not a finding. The M6 comparison needs the shell actually demoted, and until then the honest claim from this run is narrower and still worth having: the attribution works, the per-surface budgets can be read at last, and the tail behaviour above is real | 2026-09-11, measured on hardware |


Read the sealed rows for what they are. `shell_surface/conftest.py` guarantees a
poweroff/format/nmcli test can never touch the host, and chapter 00 asserts that
seal. So those 449 exercise the REAL handlers with the OS calls faked. They are
not hardware, and a row here never becomes one; what they rule out is a whole
class of crash and regression before hardware is involved.

## Unverified

Ordered by how much a contributor with modest hardware can settle in an evening.

| # | Claim | What would settle it | Hardware |
|---|---|---|---|
| 1 | The Pi 4 image boots | Flash `hart-edge`, boot, paste `journalctl -b` | Pi 4, any RAM |
| 2 | GPIO actually toggles from the agent | `integrations/channels/hardware/gpio_adapter.py` against a real pin and an LED | Pi 4 + LED |
| 3 | The CPU-only path is usable, not just present | tokens/sec for the 2B on a Pi 4 8 GB, no GPU | Pi 4, 8 GB |
| 4 | A 4 GB board degrades honestly | Confirm it lands in LITE and says so rather than failing to start a model | Pi 4, 4 GB |
| 5 | Serial adapter drives real hardware | `serial_adapter.py` to any USB-serial device | any board |
| 6 | The ROS 2 bridge talks to a real node | `ros_bridge.py` publish/subscribe against a running ROS 2 graph | ROS 2 install |
| 7 | Two nodes find each other and borrow compute | Two machines, `hart hive connect`, one borrows from the other | 2 machines |
| 8 | A borrow settles what it owes | End-to-end through `compute_borrowing.py` and `revenue_aggregator.py` | 2 machines |
| 9 | The 19 nixosTests pass | `nixos-vm-tests.yml` is manual-dispatch and has no passing run | KVM host |
| 10 | The co-pilot completes one loop on a node | Task picked up, config activated, PR opened. Its verification step was broken until `21acfecb` | installed node |
| 11 | PinePhone boots | `nixos/hardware/pinephone.nix` | PinePhone |
| 12 | RISC-V boots | `nixos/hardware/riscv-generic.nix` | RISC-V board |
| 13 | A Reachy Mini runs an agent locally | Drive it through `gpio_adapter.py` / `serial_adapter.py` / `ros_bridge.py`, whichever its stack exposes | Reachy Mini |
| 14 | **Intelligence compounds between two robots** | Robot A learns a skill by doing. Robot B performs it without having done it. Measure B before and after | 2 Reachy Minis |
| 15 | A co-pilot's work on one node improves another | Seed a goal on node A only. Show node B starting ahead of where it was | 2 nodes |
| 16 | Three nodes beat one node | Same benchmark, one node then three, same models. A floor test: error minimisation, not a capability claim | 3 machines |
| ~~17~~ | SETTLED 2026-09-07, see the Verified table. Full cycle run on the box: fetch from the cache, apply, boot generation 2, roll back to generation 1 | done | 1 reflashed node |
| 18 | An app installs through the SHIPPED SERVICE and launches | The nix platform is now verified above, driven as the session user. Not yet driven through `hart-liquid-ui`'s own routes as `hart` (its profile and nix-daemon access are untested), and the flatpak / appimage / android platforms remain faked in tests only | installed node |
| 19 | Input-to-photon meets the per-surface budgets | NOT a human problem, and not a broken instrument. Established on the box 2026-09-07: the instrument IS compiled into the running binary (the `hart-latency` string is present in it), synthetic input DOES reach the compositor (104 chords, each logged `workspace.switched`), and it emitted zero samples anyway. Cause: T_photon never occurs. Every switch logged `restored=0` because there are no toplevel windows, so it changed no pixels; `layer.composited` has fired ONCE this boot. latency.rs pairs an input with the NEXT page flip, so if the display were flipping at all, all 104 chords and the earlier 2228 pointer moves would have produced samples. Zero samples is proof the screen is static. What is needed is an input that causes a REPAINT: a native toplevel to arrange (app-launch is the uid wall, DROPPED by owner) or a hover that makes the WebKit shell damage its surface | a node with a native toplevel, or a hover target that repaints |
| 20 | A drop line never appears on a healthy box | Same journal. `hart-latency dropped ... verdict=SUSPECT` means vblanks stopped being reaped or frames stopped being queued, and the numbers beside it cannot be trusted | node with a GPU |
| 21 | A recipe replays THROUGH THE SHIPPED SERVICE | The replay itself is now verified above, driven as the session user. What is NOT verified is the same thing from `hart-liquid-ui`, which runs as `hart` and reaches the compositor only through the `hart-comp-ipc` relay + `HART_COMP_SOCK` added in 94294ef. Those units parse and are unit-tested but have never run: this node cannot build a generation locally and OTA needs a pushed commit | a node running a build that carries 94294ef |

| 22 | A copilot task writes real files | BLOCKED AT THE CREDENTIAL, and now precisely. Proven present on the box: `hart-copilot-daemon` alive and polling every 5 min, correctly logging `no task assigned by the hive`; the hive session live on :6777 with `can_edit_files`/`can_run_commands`; the `claude` binary on the daemon's own PATH. Missing: any Anthropic credential. No API key in the unit (only `ANTHROPIC_MODEL=claude-sonnet-5`), no credential file for the daemon user, no EnvironmentFile. `claude -p` then HANGS rather than failing: 0 bytes on stdout AND stderr, killed at 45s. The daemon bounds it with TASK_TIMEOUT_S so it reports a timeout, but it can never succeed. Note `gh` is also unauthenticated, so the push/PR step cannot reach GitHub either | a node with an Anthropic credential provisioned |
| 23 | Screen capture works under Tier-1 | hart-comp does NOT implement `wlr-screencopy-unstable-v1`: `grim` refuses and `/api/shell/screenshot` returns 403 while the native tier is live. The parity matrix's screen-capture row is green for the sway/cage tiers only | node with a GPU |
| 24 | The native scene is FASTER than the WebView it replaces | Half of this row is settled above: the scene draws on a real display and the instrument attributes to it. What is not settled is the comparison, because `shell.native` leaves the WebView running, so the native half was measured on top of it rather than instead of it. Settling it needs the shell demoted, which is M6's obligation 3 (how chrome state reaches the compositor) plus a native writer for `shell-ready`, obligation 1, which is done. Then the same sweep, same five points, same boot | a node whose shell stands down |

Row 7's first half is now settled: two nodes DO find each other, see the PeerLink
and announce rows above. What is still open there is the compute borrow itself.

### What "the hive learns" means today, precisely

**Active now (Phase 1).** `federated_aggregator.py` runs four channels: embedding
deltas (`embedding_delta.py` + `gradient_service.py`), model lifecycle deltas,
resonance tuning, and recipe sharing, which the code calls "trained task
intelligence". `world_model_bridge.py` turns agent interactions into training
data and distributes skills by gossip and RALT ingestion. This is real learning
at the retrieval, skill and routing layer, and it is what compounds today.

**Not active (Phase 2).** `federated_gradient_protocol.py` says so in its first
line: "Phase 2 stubs for LoRA gradient sync". Byzantine-resilient aggregation,
differential privacy, gradient compression, all interface definitions and
placeholders.

**Where the learning itself lives.** Not in this repo. Hebbian, Bayesian and
gradient work is in HevolveAI, a private sibling, loaded at runtime as a
signature-verified binary with a stub fallback. The seam is
`security/native_hive_loader.py`, and the README says so under a heading that
calls it the part we are not comfortable with.

So the accurate split is: HevolveAI learns, this repo carries what it derives
between nodes over four channels, and node-to-node weight sync is Phase 2.

### Row 16 is a floor, not a threshold

`hive_benchmark_prover.py` carries a seven-stage convergence ladder, from one
node at roughly 62% MMLU to a hundred thousand nodes past any single model.
Stage 2, three nodes ensembling, is where it claims the sum first exceeds the
single.

That stage measures error minimisation. Three models voting reduce variance and
catch each other on a given answer, which is well established and says nothing
about capability. If three nodes do not beat one, something is broken. If they
do, ensembling works, which nobody disputes.

The capability claim lives in mechanisms stage 2 does not exercise: expert
routing across models with different blind spots, generate-review-test where
verification is separable from generation, and learning that compounds so the
next node starts ahead. Rows 14 and 15 test those. Row 16 only shows the floor
exists.

**Every number in that ladder is a projection.** Not one has been measured, and
the source now says so.

Three laptops answer it. No robot, no Pi, no GPU rack.

**How the node count gets there:** crowdsourced compute, from users and
developers lending what is idle. `compute_borrowing.py` advertises and settles,
`revenue_aggregator.py` splits 90/9/1 and tracks payouts. That is the bootstrap
path, and it only starts paying for itself past this row.

### Rows 14 and 15 are the thesis

Rows 1 to 13 are plumbing. These two are the claim, and neither is demonstrated.

The mechanism is written. `integrations/agent_engine/world_model_bridge.py`:
"Every agent interaction becomes training data for continuous learning. Skills
distribute via gossip notification + local RALT ingestion." Experiences pass
`ConstitutionalFilter` before storage, RALT export is rate-limited and witnessed
by `WorldModelSafetyBounds`, and skill packets go through `ConstructiveFilter`.
`federated_aggregator.extract_local_delta()` pulls learning stats from the bridge
and signs them with the node identity.

So the path from one robot's experience to another robot's competence exists as
code, end to end. Nobody has watched it happen.

**What would settle row 14.** Not two robots taking turns answering, which is
only failover and proves nothing about learning. Robot A acquires something by
doing it. Robot B, which never did it, is then measurably better at it than it
was before. The before-and-after is the whole test: without a baseline it is a
story.

**What would settle row 15.** Same shape, one layer up. The co-pilot daemon works
a seeded goal on node A. Node B, given a related goal, starts from a better place
than it would have. This is what "the hive bootstraps itself" means, and it is
either measurable or it is a slogan.

A negative result here is worth more than any passing row above. If joint
experience does not compound, the architecture needs to change and everyone
should know.

### The honest state of these

Rows 1 to 6 are written and never confirmed on the metal. The code exists; that
is a different claim from the code working, and today produced a reminder: the
co-pilot's `nixos-rebuild` step had never once run on any machine because the
unit could not use `sudo`, and nothing caught it because nothing ran it.

Row 9 is worse than unverified. Four shards fail on every push to `main`, and
have long enough that the red is read as noise.

Rows 7 and 8 are the ones that decide whether the whole design works. The hive
is supposed to bootstrap itself: a device too small to think borrows compute,
the lender is paid, payment makes lending worth doing. Every part of that exists
in code. None of it has been demonstrated between two machines owned by
different people.

That is the experiment. It needs more than one person's hardware, which is why
it is written down here rather than claimed in the README.

## Rules for this file

- A row moves up only with output pasted in the issue. Not a description of
  output.
- A failure moves up too, recorded as a failure. A file that only ever gains
  passes is not being read honestly.
- If a claim in the README is not in either table, it should not be in the
  README.
