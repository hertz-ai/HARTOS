{
  "upstream": "https://github.com/instructkr/claw-code",
  "pinned_commit": "7030d26e7a9ca7fef5c74f463eede01a59403847",
  "pinned_date": "2026-04-01",
  "license": "MIT",
  "license_verified_at_pin": true,
  "reason": "hart-bridge PyO3 crate depends on runtime::execute_bash, read_file, write_file, edit_file, glob_search, grep_search signatures. Upstream changes to these function signatures break the bridge.",
  "bridge_crate": "rust/crates/hart-bridge",
  "compatibility_test": "tests/unit/test_claw_compat.py",
  "provenance": {
    "source_hash_sha256": "computed at pin time — verify with: git -C claw_native rev-parse HEAD",
    "bridge_source_hash": "sha256 of hart-bridge/src/lib.rs at pin time",
    "binary_reproducible": false,
    "note": "We vendor source, not binary. The .pyd is built locally from pinned source. Provenance chain: pinned commit → local cargo build → .pyd in repo root."
  },
  "upgrade_procedure": [
    "1. git -C claw_native fetch origin",
    "2. git -C claw_native log --oneline origin/main..HEAD  (review what changed)",
    "3. git -C claw_native merge origin/main",
    "4. Check LICENSE still MIT: head -1 claw_native/rust/LICENSE",
    "5. cargo check --package hart-bridge  (Gate 1: API compat)",
    "6. cargo build --release --package hart-bridge  (rebuild .pyd)",
    "7. cp claw_native/rust/target/release/claw_bridge.dll claw_bridge.pyd",
    "8. pytest tests/unit/test_claw_compat.py  (Gate 2: FFI + Gate 3: pin)",
    "9. If all pass: update pinned_commit in this file",
    "10. git add claw_native/PINNED.json claw_bridge.pyd && git commit"
  ]
}
