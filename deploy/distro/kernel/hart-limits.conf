# HART OS Process Limits
# Install to /etc/security/limits.d/hart-limits.conf

hart    soft    nofile    65536
hart    hard    nofile    65536
hart    soft    nproc     4096
hart    hard    nproc     4096
hart    soft    memlock   unlimited
hart    hard    memlock   unlimited
