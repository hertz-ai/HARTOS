# HART OS Desktop Variant Configuration
# GNOME minimal + D-Bus + system tray + browser dashboard
# Target: workstations, developer machines

[variant]
name = desktop
description = HART OS Desktop — Node with GUI dashboard and system tray
base = ubuntu-desktop-minimal-22.04

[packages]
# Core
include = python3.10 python3.10-venv python3-pip ufw git curl rsync xxd htop

# Desktop integration
include = python3-dbus python3-gi policykit-1 gnome-shell-extensions
include = python3-pystray python3-pil

# Browser for dashboard
include = firefox

# No heavy office/media packages
exclude = libreoffice* thunderbird* rhythmbox* shotwell*

[services]
hart-backend = enabled
hart-discovery = enabled
hart-agent-daemon = enabled
hart-vision = enabled-if-model
hart-llm = disabled-by-default
hart-dbus = enabled

[desktop]
# System tray autostart
autostart = hart-dashboard.desktop
# D-Bus service
dbus_service = com.hart.Agent.conf
# Polkit policy
polkit_policy = com.hart.agent.policy

[resources]
min_ram_gb = 8
min_disk_gb = 20
# Leave resources for desktop usage
max_cpu_percent = 75
