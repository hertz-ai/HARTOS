{
    "status": "done",
    "action": "Store all 5 generated videos under the source conv_id with metadata including: genre, assigned avatar, video URL, generation timestamp, and expiry timestamp (+2 hours).",
    "fallback_action": "",
    "persona": "News Curator",
    "action_id": 7,
    "recipe": [
        {
            "steps": "Retrieve the video URLs, generation timestamps, avatar assignments, and source conv_id from memory.",
            "tool_name": "get_data_by_key",
            "agent_to_perform_this_action": "Helper"
        },
        {
            "steps": "For each video, calculate the expiry timestamp by adding 2 hours to the generation timestamp.",
            "generalized_functions": "from datetime import datetime, timedelta\n\ndef calculate_expiry(generation_time_string):\n    \"\"\"\n    Calculate the expiry timestamp by adding two hours to the generation timestamp.\n    Args:\n        generation_time_string (str): The generation time in ISO format (YYYY-MM-DDTHH:MM:SS).\n    Returns:\n        str: The expiry timestamp in the same format.\n    \"\"\"\n    gen_time = datetime.fromisoformat(generation_time_string)\n    expiry = gen_time + timedelta(hours=2)\n    return expiry.isoformat()\n\n# Example usage:\n# expiry_time = calculate_expiry(\"2024-06-16T13:30:00\")",
            "agent_to_perform_this_action": "Executor"
        },
        {
            "steps": "Consolidate genre, avatar, video URL, generation timestamp, and expiry timestamp into a structured dictionary, grouped under the source conv_id.",
            "generalized_functions": "def consolidate_video_metadata(source_conv_id, video_data):\n    \"\"\"\n    Organizes video metadata for storage under the source conv_id.\n    Args:\n        source_conv_id (str): Global conversation identifier.\n        video_data (list): List of dicts with keys genre, avatar, url, generation, expiry.\n    Returns:\n        dict: Metadata dictionary keyed by source_conv_id.\n    \"\"\"\n    return {source_conv_id: video_data}\n\n# Example usage:\n# meta = consolidate_video_metadata('source123', [{\"genre\":\"Politics\",...}])",
            "agent_to_perform_this_action": "Executor"
        },
        {
            "steps": "Store the consolidated metadata structure in memory under a generic key path for video information.",
            "tool_name": "save_data_in_memory",
            "agent_to_perform_this_action": "Helper"
        }
    ],
    "can_perform_without_user_input": "yes",
    "scheduled_tasks": [],
    "metadata": {
        "user_id": "redacted <class 'int'>",
        "conv": {
            "source_window": "redacted <class 'str'>"
        },
        "avatars": {
            "teacher_avatar_ids": "redacted <class 'str'>"
        },
        "genres": {
            "avatar_assignment": "redacted <class 'str'>"
        }
    },
    "time_took_to_complete": 22.327492237091064,
    "actions_this_action_depends_on": [
        6
    ]
}