{
    "status": "done",
    "action": "Store metadata for all 7 generated news videos, including genre, assigned avatar, video URL, generation timestamp, and expiry timestamp (set +2 hours from generation).",
    "fallback_action": "Retry storing video metadata up to three times if a failure occurs.",
    "persona": "News Curator",
    "action_id": 7,
    "recipe": [
        {
            "steps": "Retrieve video generation info for all genres, including conv_id, avatar assignments, and timestamps, from memory.",
            "tool_name": "get_data_by_key",
            "agent_to_perform_this_action": "Helper"
        },
        {
            "steps": "For each video that has succeeded, obtain the video URL using its conv_id (from previous status checks or API).",
            "tool_name": "",
            "agent_to_perform_this_action": "Assistant"
        },
        {
            "steps": "For each video, construct a metadata object with the following fields: genre, avatar_id, video URL, generation timestamp, and expiry timestamp (+2 hours from the generation timestamp).",
            "tool_name": "",
            "generalized_functions": "def generate_video_metadata(videos_by_genre):\n    '''\n    For each video entry, create a metadata object with all required fields and an expiry time.\n    Args:\n        videos_by_genre (dict): Genre as key, dict containing avatar_id, video URL, and generation timestamp as values.\n    Returns:\n        list: List of metadata dictionaries for all videos.\n    '''\n    from datetime import datetime, timedelta\n    metadata_list = []\n    for genre, info in videos_by_genre.items():\n        gen_time = datetime.fromisoformat(info['generation_timestamp'])\n        expiry_time = gen_time + timedelta(hours=2)\n        meta = {\n            'genre': genre,\n            'avatar_id': info['avatar_id'],\n            'video_url': info['video_url'],\n            'generation_timestamp': info['generation_timestamp'],\n            'expiry_timestamp': expiry_time.isoformat()\n        }\n        metadata_list.append(meta)\n    return metadata_list\n",
            "agent_to_perform_this_action": "Executor"
        },
        {
            "steps": "Save the complete metadata array for all videos in memory under a generic key for easy and future retrieval.",
            "tool_name": "save_data_in_memory",
            "agent_to_perform_this_action": "Helper"
        }
    ],
    "can_perform_without_user_input": "yes",
    "scheduled_tasks": [],
    "metadata": {
        "user_id": "redacted int",
        "avatars": {
            "teacher_avatar_ids": [
                "redacted int",
                "redacted int",
                "redacted int",
                "redacted int",
                "redacted int",
                "redacted int",
                "redacted int",
                "redacted int",
                "redacted int",
                "redacted int"
            ]
        },
        "genres": {
            "avatar_assignment": {
                "Politics": "redacted int",
                "Business": "redacted int",
                "Sports": "redacted int",
                "Breaking News": "redacted int",
                "Science": "redacted int",
                "AI": "redacted int",
                "Weather": "redacted int"
            }
        }
    },
    "time_took_to_complete": 15.67261528968811,
    "actions_this_action_depends_on": [
        6
    ]
}